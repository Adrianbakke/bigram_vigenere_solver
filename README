installer requirements med:
    pip install -r requirements.txt

kjør med:
    python3 solver.py <cipher> <key>

eksempel:
    python3 solver.py 'this text is encrypted with the vigenere cipher breaking it is rather easy' hello

